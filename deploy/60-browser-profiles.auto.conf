# Deprecated compatibility filename; intentionally contains no systemd directives.
# Browser-profile host activation is a separate schema/deployment migration and
# must not be bundled with transport normalization. Use the transport-only
# 60-browser-transport.auto.conf after preserving the existing supervisor config.
