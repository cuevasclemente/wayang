# Reference systemd drop-in for an existing supervised Wayang deployment.
# `auto` is the production standard: Linux selects Full browser when Xvfb and
# x11vnc are executable; unsupported hosts retain the Fast page fallback.
[Service]
Environment=WAYANG_BROWSER_TRANSPORT=auto
